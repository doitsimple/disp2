var auth = require('./controllers/auth');
^^if(global.code){
for( var key in global.code){$$
var ^^=key$$ = require("./controllers/^^=key$$");
^^}}$$
var passport = require("passport");
// Use the passport package in our application
app.use(passport.initialize());
